# Release Notes

Track shipped UX/features and map each one to the commit that introduced it.

## 2026-03-24

| Area | UX / Feature change | Commit ID |
| --- | --- | --- |
| Release metadata | Sidebar now shows runtime commit SHA and adds one-click **Refresh status + release metadata** action. | `TBD` |
| Delivery workflow | Added CI guard to verify runtime commit SHA visibility in app UI before release. | `TBD` |
| Deployment process | Added release workflow and deployment checklist with merged/deployed/sidebar SHA verification. | `TBD` |

> Replace `TBD` with actual commit IDs as part of merge/release.
